// Code generated by ent, DO NOT EDIT.

package ent

import (
	"backend/ent/objectstorage"
	"backend/ent/panier"
	"backend/ent/panierobjectstorage"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// PanierObjectStorage is the model entity for the PanierObjectStorage schema.
type PanierObjectStorage struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Quantity holds the value of the "quantity" field.
	Quantity int `json:"quantity,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PanierObjectStorageQuery when eager-loading is set.
	Edges                        PanierObjectStorageEdges `json:"edges"`
	objectStorage_id             *int
	panier_panier_object_storage *int
	selectValues                 sql.SelectValues
}

// PanierObjectStorageEdges holds the relations/edges for other nodes in the graph.
type PanierObjectStorageEdges struct {
	// Panier holds the value of the panier edge.
	Panier *Panier `json:"panier,omitempty"`
	// ObjectStorage holds the value of the objectStorage edge.
	ObjectStorage *ObjectStorage `json:"objectStorage,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// PanierOrErr returns the Panier value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PanierObjectStorageEdges) PanierOrErr() (*Panier, error) {
	if e.loadedTypes[0] {
		if e.Panier == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: panier.Label}
		}
		return e.Panier, nil
	}
	return nil, &NotLoadedError{edge: "panier"}
}

// ObjectStorageOrErr returns the ObjectStorage value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PanierObjectStorageEdges) ObjectStorageOrErr() (*ObjectStorage, error) {
	if e.loadedTypes[1] {
		if e.ObjectStorage == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: objectstorage.Label}
		}
		return e.ObjectStorage, nil
	}
	return nil, &NotLoadedError{edge: "objectStorage"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PanierObjectStorage) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case panierobjectstorage.FieldID, panierobjectstorage.FieldQuantity:
			values[i] = new(sql.NullInt64)
		case panierobjectstorage.ForeignKeys[0]: // objectStorage_id
			values[i] = new(sql.NullInt64)
		case panierobjectstorage.ForeignKeys[1]: // panier_panier_object_storage
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PanierObjectStorage fields.
func (pos *PanierObjectStorage) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case panierobjectstorage.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pos.ID = int(value.Int64)
		case panierobjectstorage.FieldQuantity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field quantity", values[i])
			} else if value.Valid {
				pos.Quantity = int(value.Int64)
			}
		case panierobjectstorage.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field objectStorage_id", value)
			} else if value.Valid {
				pos.objectStorage_id = new(int)
				*pos.objectStorage_id = int(value.Int64)
			}
		case panierobjectstorage.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field panier_panier_object_storage", value)
			} else if value.Valid {
				pos.panier_panier_object_storage = new(int)
				*pos.panier_panier_object_storage = int(value.Int64)
			}
		default:
			pos.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PanierObjectStorage.
// This includes values selected through modifiers, order, etc.
func (pos *PanierObjectStorage) Value(name string) (ent.Value, error) {
	return pos.selectValues.Get(name)
}

// QueryPanier queries the "panier" edge of the PanierObjectStorage entity.
func (pos *PanierObjectStorage) QueryPanier() *PanierQuery {
	return NewPanierObjectStorageClient(pos.config).QueryPanier(pos)
}

// QueryObjectStorage queries the "objectStorage" edge of the PanierObjectStorage entity.
func (pos *PanierObjectStorage) QueryObjectStorage() *ObjectStorageQuery {
	return NewPanierObjectStorageClient(pos.config).QueryObjectStorage(pos)
}

// Update returns a builder for updating this PanierObjectStorage.
// Note that you need to call PanierObjectStorage.Unwrap() before calling this method if this PanierObjectStorage
// was returned from a transaction, and the transaction was committed or rolled back.
func (pos *PanierObjectStorage) Update() *PanierObjectStorageUpdateOne {
	return NewPanierObjectStorageClient(pos.config).UpdateOne(pos)
}

// Unwrap unwraps the PanierObjectStorage entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pos *PanierObjectStorage) Unwrap() *PanierObjectStorage {
	_tx, ok := pos.config.driver.(*txDriver)
	if !ok {
		panic("ent: PanierObjectStorage is not a transactional entity")
	}
	pos.config.driver = _tx.drv
	return pos
}

// String implements the fmt.Stringer.
func (pos *PanierObjectStorage) String() string {
	var builder strings.Builder
	builder.WriteString("PanierObjectStorage(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pos.ID))
	builder.WriteString("quantity=")
	builder.WriteString(fmt.Sprintf("%v", pos.Quantity))
	builder.WriteByte(')')
	return builder.String()
}

// PanierObjectStorages is a parsable slice of PanierObjectStorage.
type PanierObjectStorages []*PanierObjectStorage
